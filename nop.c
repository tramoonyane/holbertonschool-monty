#include "monty.h"
/**
 * f_nop - Does nothing
 * @head: Double pointer to the head of the stack (unused)
 * @counter: Line number counter for potential error messages (unused)
 *
 * Description: This function does nothing.
 * It is a placeholder or "no operation"
 * function that doesn't affect the stack.
 * It takes pointers to the head of the
 * stack and a counter for error messages as parameters
 * but doesn't use them.
 *
 * @head: Pointer to the head pointer of the stack (unused)
 * @counter: Line number (unused)
 */
void f_nop(stack_t **head, unsigned int counter)
{
(void) counter;
(void) head;
}
